apiVersion: v1
kind: Pod
metadata:
  name: roboshop-resource-limiting

spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        requests:
          memory: "68Mi"
          cpu: "100m"
        
        limits:
          memory: "128Mi"
          cpu: "150m"